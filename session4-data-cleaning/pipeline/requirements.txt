# Session 4 data-cleaning pipeline.
# The cleaning strategies themselves are implemented from scratch (pure stdlib):
# language-ID, Unicode normalization, MinHash/LSH dedup, quality heuristics,
# PII/toxicity/secret scanning and corpus rebalancing. The only third-party
# dependency is pyarrow, used purely to *read* the source Wikipedia parquet
# shards (and requests, to download them).
pyarrow>=14
requests>=2.28
regex>=2023.0   # Unicode-property (\p{L}\p{M}\p{N}) tokenizing for Indic scripts
